[project]
name = "lm-query-examples"
version = "0.1.0"
description = "Minimal examples for querying local and cloud LLMs from Python"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "torch>=2.5.0",
    "transformers>=4.51.0",
]
